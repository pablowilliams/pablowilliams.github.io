# Contributing

This is a personal portfolio, but corrections to broken links, accessibility issues and reproducibility defects are welcome.

1. Create a branch from `main`.
2. Run `npm ci` and `npx playwright install chromium`.
3. Make the smallest focused change.
4. Run `npm run verify`.
5. Open a pull request describing the visible effect and the verification performed.
